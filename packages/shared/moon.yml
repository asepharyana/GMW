$schema: 'https://moonrepo.dev/schema/config/v2.json'

language: typescript

tasks:
  typecheck:
    command: tsc --noEmit
    options:
      runFromWorkspaceRoot: false
    inputs:
      - src
      - tsconfig.json
